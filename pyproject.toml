[project]
authors = [{name = "chaochen", email = "chaochen820@gmail.com"}]
dependencies = []
name = "clinical-trial-patient-matching"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
clinical_trial_patient_matching = { path = ".", editable = true }

[tool.pixi.tasks]

[tool.pixi.dependencies]
python = "3.11.*"
pytest = ">=9.1.1,<10"
ruff = ">=0.16.7,<0.17"
pre-commit = ">=4.4,<5"
